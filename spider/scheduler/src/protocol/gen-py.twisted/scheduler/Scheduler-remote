#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:twisted
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import Scheduler
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  void ping()'
  print '  void do_register(string spiderid)'
  print '  void do_unregister(string spiderid)'
  print '  SeedsPackage get_seeds(string spiderid, SeedsPackage last_pkg, JobReport report)'
  print '  void add_seeds(string clientid, SeedsPackage pkg)'
  print '  i32 get_latency_time(string spiderid, string url)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = Scheduler.Client(protocol)
transport.open()

if cmd == 'ping':
  if len(args) != 0:
    print 'ping requires 0 args'
    sys.exit(1)
  pp.pprint(client.ping())

elif cmd == 'do_register':
  if len(args) != 1:
    print 'do_register requires 1 args'
    sys.exit(1)
  pp.pprint(client.do_register(args[0],))

elif cmd == 'do_unregister':
  if len(args) != 1:
    print 'do_unregister requires 1 args'
    sys.exit(1)
  pp.pprint(client.do_unregister(args[0],))

elif cmd == 'get_seeds':
  if len(args) != 3:
    print 'get_seeds requires 3 args'
    sys.exit(1)
  pp.pprint(client.get_seeds(args[0],eval(args[1]),eval(args[2]),))

elif cmd == 'add_seeds':
  if len(args) != 2:
    print 'add_seeds requires 2 args'
    sys.exit(1)
  pp.pprint(client.add_seeds(args[0],eval(args[1]),))

elif cmd == 'get_latency_time':
  if len(args) != 2:
    print 'get_latency_time requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_latency_time(args[0],args[1],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
